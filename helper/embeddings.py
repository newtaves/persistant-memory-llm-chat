#Contains helper functions to create and manage embeddings for the documents and chat sessions.
#
#

def get_context(keywords, top_k):
    """
    use the keywords to search for the relevant chunks in the vector database and return the top_k chunks as context
    """
    return 1

